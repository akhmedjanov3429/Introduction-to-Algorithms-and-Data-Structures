## Auto-generated by gen_makefile.py

PORT=9997
CLIENT = "sag_client"
#SERVER = 10.200.20.76
#SERVER = dku-cscourse-01.oit.duke.edu  
SERVER = 10.200.14.50
TARGET = Permutation_v1.java
COMPILE = javac
.PHONY: submit client info status compile init-pull

info:
	@echo "Usage: make submit|info|client|status"

compile:
	/bin/rm -f *.class
	${COMPILE} ${TARGET}

submit: client
client:
	@${CLIENT} --port ${PORT} . --cmd submit --server ${SERVER}

status:
	@${CLIENT} --port ${PORT} . --cmd status --server ${SERVER}

hw_stat:
	@${CLIENT} --port ${PORT} --cmd hw_stat --server ${SERVER}

rpt:
	@${CLIENT} --port ${PORT} --cmd status --server ${SERVER} all

init-pull:
	@${CLIENT} --port ${PORT} --cmd init-pull --server ${SERVER} .
